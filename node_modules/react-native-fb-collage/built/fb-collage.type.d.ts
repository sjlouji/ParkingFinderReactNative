import { ImageResizeMode, ViewStyle, TextStyle } from 'react-native';
export declare type FuncStyle = {
    (length: number): ViewStyle;
};
export declare type StaticStyle = {
    [x: string]: ViewStyle | TextStyle | FuncStyle;
};
declare type ImageRequireSource = number;
declare type ImageURISource = string;
export declare type ImageSource = ImageRequireSource | ImageURISource;
export declare class Props {
    images: ImageSource[];
    resizeMode: ImageResizeMode;
    height: number;
    width: number;
    borderRadius: number;
    spacing?: number;
    style?: ViewStyle;
    overlayStyle?: ViewStyle;
    textStyle?: TextStyle;
    imageOnPress?: {
        (index: number, images: any[]): void;
    };
}
export {};
